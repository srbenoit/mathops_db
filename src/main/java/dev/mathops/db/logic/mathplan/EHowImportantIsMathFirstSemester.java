package dev.mathops.db.logic.mathplan;

/**
 * An indication of how important it is for students to register for math in their first semester.
 */
public enum EHowImportantIsMathFirstSemester {

    /** Math is very important. */
    VERY_IMPORTANT,

    /** Math is important. */
    IMPORTANT,

    /** Math is recommended. */
    RECOMMENDED,

    /** Math is not important. */
    NOT_IMPORTANT,
}
