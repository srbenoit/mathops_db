/**
 * Record classes that represent the raw (uninterpreted) records from the database. For example, a flag field stored as
 * 'Y' or 'N' may be interpreted as a Boolean in the data object, but here it's simply represented as a String.
 *
 * <p>
 * These records are intended for handling raw operations on tables like archiving data or copying between databases.
 */
package dev.mathops.db.old.rawrecord;
